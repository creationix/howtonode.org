.post
  .details
    %p(title=date)&= format_date(date, "%Y.%m.%d")
    %p
      %a(href=author.link)&= author.name
  .body
    %h1
      %a(href=link)&= title
    :if typeof full !== 'undefined'
      = content
    :if typeof full === 'undefined'
      = content.split("<h2>")[0]
      %p
        %a(href=link) Read Full Story...
